services:
  - type: web
    name: epa-feedback-app
    runtime: node
    plan: starter
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: RESPONSES_DIR
        value: /var/data
    disk:
      name: epa-feedback-data
      mountPath: /var/data
      sizeGB: 1
